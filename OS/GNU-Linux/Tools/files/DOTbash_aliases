# My custom aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# Sys Admin related stuff
alias sys_cleanup="sudo apt-get clean && sudo apt autoremove"
alias sys_shutdown="sudo shutdown -h now"
alias sys_reboot="sudo reboot"
